# Personal AI Hub 需求说明

## 1. 目标

将用户的 AI 历史对话整理为可分类、可检索、可回溯、可维护的本地个人知识库，并通过网页智能体查询。

## 2. 技术约束

- Next.js + TypeScript。
- 使用 grep/ripgrep 和规则进行检索，不使用 embedding 或向量数据库。
- 真实原始数据和生成数据默认只保存在用户本地。

## 3. 数据导入

- 数据源根目录支持子目录和 `.json / .md / .txt` 文件。
- JSON 当前支持 DeepSeek conversation mapping 导出结构。
- Markdown/TXT 支持 Gemini `Q/A` 标记。
- 无 Q/A 标记的 TXT 作为一条原始 conversation 导入。
- 标准化后保留来源 AI、文件、对话/消息 ID、时间、行号和 URL 等路由信息。

## 4. Project 分类

- 输入阶段将对话分类到不同 Project。
- 使用方案 B：基于可维护规则和关键词的保守路由。
- 无法确定的内容进入 `unassigned`。

## 5. Memory

- 同时使用方案 A + B：保留 conversation 原文切片，并规则抽取 `decision / task / problem / knowledge`。
- 记忆包含 Project、类型、主题、内容、关键词、日期、来源 AI、来源路由、置信度和状态。
- 支持更新、合并、替代、废弃及查看历史状态。

## 6. 检索与回答

1. 优先搜索当前 Project 的分类知识库。
2. 追问或细节不足时，按来源路由回查原始数据。
3. 多源证据冲突时，通过工具封装的两个 LLM 进行有限轮争辩。
4. 到达轮数限制仍不收敛时，基于证据强制选择最佳支持答案并标识不确定性。
5. 本地证据不足时，可调用外部搜索工具。

## 7. 网页

- Project 列表、切换和重新导入。
- 项目内查询、全库查询和外部搜索入口。
- 展示答案、证据、路由和冲突处理结果。
- Memory 卡片显示日期、来源 AI、类型、状态和置信度。
- 支持查看原文以及 Memory 维护操作。

## 8. GitHub 发布和隐私

- 公开仓库只包含代码、文档和脱敏 Gemini/通用文本示例。
- DeepSeek 数据、真实 Gemini 数据、`data/`、`.env*` 和本地数据库不得提交。
- 体验者数据默认保存在体验者自己的设备。
- 如改成公网多用户服务，上线前必须增加鉴权、用户隔离、加密、删除机制和隐私说明。

## 9. 当前边界

- 已实现主要本地数据流、网页和 Memory 维护。
- 双 LLM 争辩和外部搜索已有工具接口，但尚未接入真实服务。

## 10. 验收

- 数据源目录可导入受支持的 JSON、MD 和 TXT。
- 仅提供 Gemini MD/TXT 时也可完成导入，不依赖 DeepSeek 文件。
- 日期查询可找到相应记忆，记忆卡可显示日期和来源 AI。
- 查看原文可按路由定位内容。
- Memory 变更后的状态参与后续检索。
- `pnpm run typecheck` 和 `pnpm run build` 通过。
