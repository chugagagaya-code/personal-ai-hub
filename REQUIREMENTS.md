# 个人人工智能中心需求说明

## 1. 产品目标

把不同 AI 平台的历史对话整理成一个本地、可搜索、可回溯、可人工修正的个人知识中心，并通过网页智能体完成跨时间、跨主题问答。

## 2. 核心原则

- 本地优先：原始聊天与派生数据默认不离开用户设备。
- 可追溯：每条 Memory 尽量保留到原文件、会话和消息的路由。
- 可修正：用户可以更新、移动、替代、合并或废弃自动结果。
- 文本召回：使用 `rg`/grep，不使用 embedding 或向量数据库。
- 有证据回答：答案基于实际召回内容，证据不足时明确说明。

## 3. 技术要求

- Next.js + React + TypeScript。
- Node.js 22.5+（推荐 24）、pnpm 9+、系统安装 ripgrep。
- JSONL/Markdown 文件库 + Node 内置 SQLite。
- 图表使用原生 HTML/CSS/SVG，不引入图表库。

## 4. 数据导入与标准化

- 数据源目录可递归读取 `.json / .md / .txt`。
- JSON 支持 DeepSeek conversation mapping；Markdown/TXT 支持 Gemini Q/A，普通 TXT 可作为单条 conversation。
- 标准化消息至少包含平台、角色、正文、时间、会话 ID、消息 ID 和来源路由。
- 无法识别的数据应跳过或记录错误，不能伪造成有效对话。

## 5. 自动主题 Project

导入时先对整批对话聚成 N 个主题簇，再逐段归簇；不能使用固定预设 Project 规则决定归属。聚类使用文本统计，不使用 embedding。

每个主题输出：

```text
projects/<id>/
├─ project.json
├─ profile.json
├─ semantic-units.jsonl
├─ memories.jsonl
└─ knowledge.md
```

`profile.json` 包含主题概述、对话数、日期跨度、核心词及支持对话数、代表对话和近期对话。用户可修改主题名称/描述并移动误归对话，修正应在重新导入后保留。

## 6. Memory 抽取与生命周期

采用“原文切片 + 规则结构化”双轨方案：`conversation` 保留原文语义单元，另抽取 `decision / task / problem / knowledge`。

Memory 包含 ID、Project、类型、状态、标题、正文、关键词、发生日期、来源 AI、来源路由、抽取方式、置信度和创建/更新时间。状态支持 `active / ignored / merged / superseded`；用户可创建、更新、废弃、合并和替代。

当前问答产生的新内容只生成候选，必须由用户确认后才写入长期记忆。

## 7. 查询与答案链路

1. 识别查询意图、范围、关键词和时间区间。
2. 优先检索当前主题；“我的全库”检索全部主题。
3. 分类库证据不足、追问或需要细节时，回查标准化原始库。
4. 仅事实性问题可在本地证据不足或明确需要最新信息时调用外部搜索。
5. 多源冲突时调用双 LLM argue，并限制轮数；不收敛时给出最佳支持结论并标记不确定性。
6. 答案包含内容简介和具体事项；明显困难时补充当前困难，跨度较长时补充前情与演变。

查询必须返回本次实际使用的证据，不得用全量或最近 Memory 冒充本次召回结果。

## 8. 网页交互

- 左侧：我的全库、自动主题、重新导入、模型配置和审计入口。
- 中间：提问、主题概览、答案及状态；默认进入“我的全库”。
- 右侧“回答依据”：当前问题使用的记忆、原文回查、外部来源和冲突结果。
- 右侧“本轮新增”：待确认候选，可保存或忽略。
- 右侧“记忆库”：全量筛选与人工维护。
- 桌面端三列分别滚动；移动端恢复页面滚动。
- 证据卡展示命中原因、相关程度、类型、日期、来源 AI，并允许查看原文。

## 9. 模型网关与外部工具

- 支持两个 OpenAI-compatible 模型槽位 A/B；未配置时本地能力仍可用。
- 密钥只保存在服务器本地目录或 `.env.local`，读取接口不得回传明文。
- 双 LLM argue 必须限制轮数并记录最终状态。
- 外部搜索只服务事实性问题，并返回可验证的 HTTP(S) 来源。

## 10. 数据库、审计与任务

- SQLite 保存审计事件、任务状态、Project 修正和对话改归。
- 重新导入通过异步任务执行，提供 pending/running/completed/failed 状态。
- 查询、外搜、模型配置和人工修正应留下审计，但不得记录 API 密钥。

## 11. 隐私与 GitHub 发布

允许公开：源代码、文档、锁文件、无密钥环境模板和人工核验的虚构/脱敏示例。

禁止提交：真实聊天导出；`data/` 下的标准化消息、主题、Memory、修正与 SQLite；`.env.local`、key、token、cookie；含姓名、联系方式、账号、私有 URL 或可识别信息的截图、日志和测试输出。

体验者的数据保存在体验者自己的设备上。若未来提供公网多用户服务，上线前必须增加认证、租户隔离、加密、数据导出/删除、配额、滥用防护与隐私政策。

## 12. 验收标准

- 脱敏示例可独立安装、导入和查询。
- DeepSeek JSON 与 Gemini MD/TXT 能正确标准化。
- 导入能生成 N 个主题及完整目录结构。
- 日期查询覆盖相应时间段，答案显示实际证据。
- 分类库不足时可回查原始库，“查看原文”能定位内容。
- Memory 与 Project 修正影响后续检索，并在重导后保留。
- 本轮问答不会未经确认写入长期记忆。
- `pnpm run typecheck` 和 `pnpm run build` 通过。
- `git ls-files` 不含真实聊天、生成数据、数据库或密钥。

## 13. 已知边界

- 自动聚类与规则抽取可能产生噪声和重复。
- 双 LLM argue 的完整多轮协议尚需继续实现和验证。
- 模型 A 外搜依赖提供商实际具备联网能力。
- 当前队列面向本地单进程，不支持多实例分布式消费。
- 当前缺少公网多用户部署需要的认证与租户隔离。
